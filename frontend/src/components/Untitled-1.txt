// frontend/src/components/Logo.jsx
// Primary EEB logo — deep blue "EEB" with green "app" subscript and a
// checkmark tucked against the last "p". Relies on DM Serif Display
// already being loaded by index.html (confirm that link includes
// :ital@0;1) — no separate font request from this component.

export default function Logo({ height = 40 }) {
  return (
    <svg
      viewBox="0 0 460 260"
      height={height}
      width={height * (460 / 260)}
      xmlns="http://www.w3.org/2000/svg"
      role="img"
      aria-label="EEB app"
    >
      <text
        x="20" y="170"
        style={{ fontFamily: "'DM Serif Display', Georgia, serif" }}
        fontSize="140"
        fill="#1B3A5C"
      >
        EEB
      </text>
      <text
        x="270" y="170"
        style={{ fontFamily: "'DM Serif Display', Georgia, serif" }}
        fontSize="76"
        fill="#2F9E6E"
      >
        app
      </text>
      <path d="M336 165 L345 174 L361 145" fill="none" stroke="#2F9E6E" strokeWidth="8" strokeLinecap="round" strokeLinejoin="round" />
    </svg>
  );
}